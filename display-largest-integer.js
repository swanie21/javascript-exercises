function compareNumbers(value1, value2) {
  if(value1 > value2) {
    console.log('The larger number is ' + value1);
  } else if(value1 < value2) {
    console.log('The larger number is ' + value2);
  } else {
    console.log('These values are the same');
  }
}

// write a JavaScript function that accepts two integers and displays the largest.
