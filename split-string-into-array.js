function splitString(string) {
  return string.trim().split('');
}

const splitString = (string) => {
  return string.trim().split('');
};

// Write a JavaScript function to split a string and convert it into an array of letters
