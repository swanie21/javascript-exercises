function checkArray(array) {
  return Array.isArray(array) === true ? 'Input is an array' : 'Input is not an array';
});

// Write a JavaScript function to check whether an `input` is an array or not.
